ktor:
  deployment:
    port: 8080
  application:
    modules:
      - com.example.shop.ApplicationKt.module

jwt:
  secret: "my-super-secret-key-for-jwt-2024"
  issuer: "http://localhost:8080/"
  audience: "shop-users"
  realm: "Shop Backend"

db:
  jdbcUrl: "jdbc:postgresql://localhost:5433/shop_db"
  user: "shop_user"
  password: "shop_password"

redis:
  host: "localhost"
  port: 6379

rabbitmq:
  host: "localhost"